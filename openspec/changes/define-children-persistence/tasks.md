## 1. Модель и чистые преобразования

- [x] 1.1 Добавить и публично экспортировать `MAX_BLOCK_DEPTH = 5`; заменить
  локальные ограничения глубины общей константой.
- [x] 1.2 Описать `FlatBlock`, `BlockPosition` и структурированные ошибки
  валидации без добавления `children` в flat-элемент.
- [x] 1.3 Реализовать и протестировать `flattenBlocks`, включая parentId,
  before/after, depth-first порядок, уникальность ID и максимальную глубину.
- [x] 1.4 Реализовать и протестировать `buildBlockTree` для произвольного порядка
  входного массива, взаимных sibling-ссылок, отсутствующих родителей, циклов,
  дубликатов и превышения глубины.
- [x] 1.5 Добавить property/fixture-тесты round trip
  `tree -> flat -> tree` и `flat -> tree -> flat` для глубины 1–5.

## 2. Ограничение вложенности

- [x] 2.1 Применить общий лимит к `canNest`/nest и горячей клавише `Tab`, не
  создавая пустых undo-шагов при отказе.
- [x] 2.2 Проверять полную относительную глубину перемещаемой ветки перед
  drag-and-drop; отклонять недопустимую цель и не показывать её как валидную.
- [x] 2.3 Реализовать чистую нормализацию структурной вставки, которая поднимает
  уровни 6+ на уровень 5 с сохранением depth-first порядка и данных блоков.
- [x] 2.4 Подключить нормализацию к block-bearing paste и импортам; доказать
  тестами, что обычный inline paste не меняет children.
- [x] 2.5 Проверить undo/redo для допустимых nest/unnest, drop и нормализованной
  вставки одной атомарной транзакцией.

## 3. Persistence boundaries

- [x] 3.1 Добавить адаптер nested `Block[]` для записи/чтения `localStorage` с
  валидацией неизвестного JSON и явным fallback на уровне потребителя.
- [x] 3.2 Сохранять в `localStorage` только публичное nested-дерево, не Tiptap JSON
  и не `FlatBlock[]`.
- [x] 3.3 Подготовить frontend backend-adapter, который принимает/возвращает
  `FlatBlock[]`; сетевые запросы, retries и mutation ID оставить за рамками.
- [x] 3.4 Добавить тест: перемещение родителя меняет flat-связи корня и затронутых
  siblings, но не создаёт изменений неизменившихся descendants.

## 4. Документация и проверка

- [x] 4.1 Создать `docs/editor/children.md` с моделью глубины, paste, nest/unnest,
  drag-and-drop, tree/flat-контрактом и критериями приёмки.
- [x] 4.2 Обновить `docs/agent.md`, `docs/architecture/project.md`,
  `docs/editor/drag-and-drop.md` и ссылки из спецификаций списков/импорта без
  дублирования общих требований.
- [x] 4.3 Выполнить `npm run lint`, `npm test`, `npm run test:types` и
  `npm run build`.
- [x] 4.4 Провести browser regression для nest/unnest и paste на границе 5/6 с
  undo/redo; drop на границе глубины и nested localStorage проверить
  интеграционными тестами соответствующих независимых адаптеров.
