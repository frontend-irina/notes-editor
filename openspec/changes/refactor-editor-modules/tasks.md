## 1. Исходное состояние

- [x] 1.1 Зафиксировать исходные lint/build и базовую версию кода для сравнения; проверка: успешные команды и commit baseline указаны в `verification.md`. По указанию пользователя браузерная проверка выделена в раздел 8.

## 2. Блоки и расширения

- [x] 2.1 Выделить `blocks/shared/block-position.ts` и `unnest-block.ts`, заменить дубликаты; проверка: единственная реализация поиска глубины и одинаковые транзакции уменьшения вложенности в Node-стенде.
- [x] 2.2 Вынести отдельные обработчики Enter paragraph, heading, quote; проверка: сравнение исходных и новых транзакций в начале/середине/конце, на пустых и вложенных блоках, с выделением, children, props и undo/redo.
- [x] 2.3 Разобрать `extensions.ts` на schema, marks, extensions и blocks/quote; проверка: сохранены имена, приоритет 1000, shortcuts; сравнения удаления пустого блока и исправления ID проходят.
- [x] 2.4 Создать фабрику `createEditorExtensions`, обновить импорты, удалить `extensions.ts`; проверка: совпадают настройки StarterKit, порядок и приоритеты extensions, TypeScript компилируется.

## 3. Сериализация

- [x] 3.1 Выделить `serialization/inline.ts`, `blocks.ts`, `index.ts`, удалить `serialization.ts`; проверка: сравнения всех шести типов блоков, inline-стилей, ссылок, вложенности, пустого содержимого и fallback проходят.
- [x] 3.2 Проверить адаптер storage со стабом localStorage; проверка: сохранение/повторная загрузка публичного JSON, повреждённый JSON и недоступное хранилище дают те же результаты, ключи хранения не изменены. Реальная браузерная проверка — в разделе 8.

## 4. Меню блоков

- [x] 4.1 Выделить состояние и команды, перевести React-представление на прямые импорты; проверка: сохранён mapping якорей, Node-стенд сравнивает query и транзакции выбора, циклических зависимостей нет.
- [x] 4.2 Выделить обработчики событий, decorations и доступность; проверка: Node-стенд сравнивает `/`, ввод запроса, Arrow/Page-навигацию, Enter и Escape; обработчики `+`, blur и ARIA перенесены без изменения тела. Проверка DOM и пользовательских событий — в разделе 8.

## 5. Перетаскивание

- [x] 5.1 Выделить типы, диапазоны, DataTransfer/preview и инкапсулированную сессию; проверка: нет экспортированной изменяемой activeDrag, сравнения одиночного и выделенного диапазонов проходят, операции DOM перенесены без изменения тела.
- [x] 5.2 Выделить drop-handler, move-selection, drag-decorations; проверка: сравнения перемещения вверх/вниз, границ, drop в исходный диапазон и между двумя моделями редактора проходят; обработчики не импортируют сборку плагина.
- [ ] 5.3 Проверить общую сессию на двух реальных экземплярах Tiptap в браузере, нативный перенос и очистку preview при drop, Escape, dragend и destroy; проверка: результаты временного браузерного стенда записаны в `verification.md`. Node-проверка транзакций с подставленной сессией этот пункт не закрывает.

## 6. Интерфейс и стили

- [x] 6.1 Выделить тему и `EditorHistoryActions`; проверка: значения темы, JSX кнопок, условия доступности и команды сохранены, редакторы остаются смонтированными согласно JSX, TypeScript проходит.
- [x] 6.2 Выделить hook выбора типа и группы кнопок toolbar; проверка: сохранены тела команд, восстановление selection, prompt и ветви отмены/удаления ссылки, общая подписка остаётся в toolbar; TypeScript проходит.
- [x] 6.3 Распределить CSS по владельцам; проверка: Node-стенд подтверждает совпадение селекторов, деклараций и условий media queries; каждый файл подключён один раз. Визуальное сравнение остаётся в разделе 8.

## 7. Документация и итоговые проверки

- [x] 7.1 Актуализировать `docs/architecture/project.md`; проверка: дерево, типы блоков, владельцы состояния и направление зависимостей соответствуют исходникам. Документы docs не добавлялись, не перемещались и не удалялись, поэтому изменение индекса `docs/agent.md` не требуется.
- [x] 7.2 Проверить размеры, импорты и границы модулей; проверка: 69 TS/TSX-файлов, циклов и неразрешённых относительных импортов нет, все файлы src TS/TSX/CSS не длиннее 150 строк.
- [x] 7.3 Выполнить итоговые lint/build и сравнительный Node-стенд; проверка: команды прошли, результаты и ограничения записаны в `verification.md`.

## 8. Отложенная браузерная проверка

- [ ] 8.1 Проверить в доступном браузере Enter и вложенность, меню через `/` и `+`, фильтрацию/пустую выдачу, composition, blur и ARIA, toolbar и ссылки, переключение вкладок, undo/redo, сохранение/перезагрузку, отсутствие новых ошибок консоли и внешний вид выше/ниже 600px; проверка: наблюдаемые результаты занесены в `verification.md`. Этот пункт отделён от реализации по прямому указанию пользователя.
